# v0.5.2 Handoff — Project Tag Hardening

Repository `/workspace/lifegrid-ai-planner-web` began at `af3416f` (merged PR #17) and contains expected preceding head `0371a1fd3eb3dae1e62797272b1e0ff361d2405b`. Baseline was APP_VERSION/package v0.5.1/0.5.1, AI interchange 4, backup schema 6. The audit found Projects in App route and BottomNav, `ProjectsView` dashboard/detail/milestone editors, dashboard helpers in `projectOperations.ts`, basic Settings controls, TaskSheet Select, `Task.projectId`, Event linked-task relationships, and milestone backup normalization.

Removed Projects route/navigation, `ProjectsView`, Project Operations health/progress/summary/next-action/grouping/detail logic, milestone UI/editor actions, and their tests. Retained Project stable IDs/name/color/aliases/order/status/notes, Task IDs, normalization, schema-6 legacy milestone normalization, backup and AI dependency behavior. Settings now uses Project Tags wording with collapsible management, search/clear, counts, reorder, edit, archive and dependency-aware delete.

Name normalization trims/collapses whitespace and rejects case-insensitive duplicates. Aliases trim/lowercase/dedupe and conflict against other names/aliases. Task selection remains optional; active tags are selectable and an already-assigned archived tag remains shown. Event tags are pure linked-Task derivations in saved order. Usage is pure and event IDs are deduplicated. Deletion plans validate final task references and atomically return full projects/tasks/events/milestones state; clear/reassign never alters Events. Legacy milestones block deletion and archive is recommended.

No browser runner was found and no manual/device/production verification was performed. Run LifeGrid tests/typecheck/build, workspace checks, and `git diff --check` before release. Known limitation: TaskSheet is a native searchable-by-keyboard select rather than a custom alias combobox; cross-browser and full UI workflows are recommended for v0.5.3. Branch `codex/implement-v0.5.2-project-tag-hardening`; commit/PR URL pending final release creation.
