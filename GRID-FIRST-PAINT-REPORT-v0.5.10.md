# Grid first-paint report v0.5.10

Marks: navigation click, route update, view render, shell commit/paint, model start/complete, first-month schedule/render/commit/paint, interaction ready, batch commits, and full-grid commit/paint/complete. `lifegridGridTiming()` computes the corresponding boundaries and records mounted count, generation, visibility, reduced-motion, service-worker control, and app version.

No browser fixture run was available in this environment, so shell/first-month/interactive/full timings, DOM counts, device/browser, longest task, and service-worker state are **unverified**. Run the acceptance procedure on a sanitized fixture before making performance claims.
