# AI prompt contract

The authoritative prompt is `aiPromptContract` in `src/lib/aiPrompt.ts`. It requires one complete raw RFC 8259 JSON v4 envelope, ASCII quotes, no prose/fences/placeholders, add/update only, stable IDs, dependency-safe same-patch parents, resolvable links, and temporal examples for all-day, timed, and unknown Events.
