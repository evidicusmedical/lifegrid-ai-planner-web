# Handoff — v0.5.28

The release adds deterministic publication profiles (`relaxed`, `standard`, `dense`), structural non-ellipsizing dates, multiline event fitting with final-line ellipsis as a last resort, time-width accounting, wrapped full-label category legends, legend-aware height, and content-bound capture geometry. The exact regression title “Drop off Natalie before Chicago trip” remains complete at representative week and moderate month-column widths. All filtered Events remain represented; infeasible safe images return the actionable filter/range error.

Renderer order is unchanged: Firefox targeted and month-column Canvas2D qualification remains intact; Chromium/WebKit retain bounded HTML paths and fallbacks. Range, category, Project, `showInExport`, theme, contrast, AI v5, and backup v7 behavior are preserved. Complete the documented browser and physical-device checks before merge review; do not merge from this handoff.

## Final convergence
Publication width now derives from shared 32 px day-column, 110 px month-column, and 24 px exterior-padding geometry. A 12-month table is 1,352 px and its border-box publication is 1,400 px, eliminating the prior unexplained trailing area. Targeted publication readiness now remains committed throughout the active PNG preview. Legacy rolling-grid qualification waits for exact semantic month keys and lazy Event-pill admission without changing production lazy rendering.
