# Local-time contract
`date` and `endDate` are plain `YYYY-MM-DD` values. `startTime` and `endTime` are nullable local `HH:MM` values. No Event calculation creates UTC instants, reads offsets, or performs DST conversion.
