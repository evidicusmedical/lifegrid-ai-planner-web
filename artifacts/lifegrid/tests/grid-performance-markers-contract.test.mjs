import test from 'node:test'; import assert from 'node:assert/strict'; import { readFileSync } from 'node:fs';
test('Grid diagnostics are development-only and complete',()=>{const app=readFileSync(new URL('../src/App.tsx',import.meta.url),'utf8'), grid=readFileSync(new URL('../src/pages/GridView.tsx',import.meta.url),'utf8'); for(const mark of ['navigation-click','route-state-updated','view-mounted','index-start','index-complete','first-commit','first-visible-cell','interaction-ready']) assert.match(app+'\n'+grid,new RegExp(`lifegrid:grid-${mark}`)); assert.match(grid,/import\.meta\.env\.DEV/);});
