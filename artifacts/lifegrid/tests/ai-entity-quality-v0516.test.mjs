import test from 'node:test'; import assert from 'node:assert/strict';
import { validateAddedEntityIdentity, resolveProposalDisplayLabel } from '../.test-build/lib/aiEntityQuality.js';
import { getPatchReadiness } from '../.test-build/lib/aiPatchApply.js';
const data={categories:[{id:'other',label:'Other',color:'#000'}],people:[],projects:[],tasks:[{id:'t1',name:'Confirm reporting instructions',category:'other',dueDate:null,status:'todo',owner:'self',priority:'medium',nextAction:null,notes:null,schedulingNotes:null,projectId:null,dueDateType:'someday-backlog',triageStatus:'ready',parentTaskId:null,linkedEventIds:[]}],events:[],personEvents:[],milestones:[]};
test('placeholder identities block while meaningful identities pass',()=>{ for(const value of ['', ' Untitled event ', 'TBD','New task','...','2026-07-21']) assert.ok(validateAddedEntityIdentity('events',{title:value})); assert.equal(validateAddedEntityIdentity('events',{title:'Apartment key pickup — time unconfirmed'}),null); });
test('sparse update labels merge authoritative identity',()=>assert.equal(resolveProposalDisplayLabel('tasks','update',{id:'t1',status:'done'},data),'Confirm reporting instructions'));
test('addition identity and empty update are blocking findings',()=>{const patch={events:{add:[{id:'e1',title:'Untitled event',date:'2026-07-21',endDate:'2026-07-21',category:'other',startTime:null,endTime:null,timeStatus:'unknown',color:'#000',linkedTaskIds:[]}],update:[]},tasks:{add:[],update:[{id:'t1'}]}};const keys=new Set(['events:add:e1','tasks:update:t1']);const codes=getPatchReadiness(data,patch,keys).findings.map(x=>x.code);assert.ok(codes.includes('PLACEHOLDER_TITLE'));assert.ok(codes.includes('UPDATE_MISSING_CHANGED_FIELDS'));});
