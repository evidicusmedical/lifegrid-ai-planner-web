# LifeGrid v0.5.22 — task workflow, settings, and events

This focused release clarifies the authoritative Task workflow, adds deterministic and explained Task sorting, makes due dates clearable, repairs persisted Project Tag and People ordering, removes the dead Project Archive action, simplifies timed-event editing to stored local values, repairs occurrence-relative all-day, timed, and approximate Event ranges plus stale untouched all-day ends, and establishes one 16-color core palette. It preserves AI interchange 4, backup schema 7, calendar isolation, atomic AI patch review/apply, historical colors, and opaque legacy event timezone fields.

See the acceptance checklist and focused contracts named `*-v0.5.22.md`.
