# LifeGrid v0.5.15.5 — Mobile export scroll repair

The mobile image-export dialog now has distinct fixed header and footer regions around an independently scrolling body. The modal is a documented global layer above application navigation, so Generate image remains available while all filters and generated-image actions remain scroll reachable. Desktop retains the existing inline export panel.
