import test from 'node:test'; import assert from 'node:assert/strict'; import { readFileSync } from 'node:fs';
test('Grid diagnostics are local development-only and retain annual DOM phases',()=>{const app=readFileSync(new URL('../src/App.tsx',import.meta.url),'utf8'), grid=readFileSync(new URL('../src/pages/GridView.tsx',import.meta.url),'utf8'), diagnostics=readFileSync(new URL('../src/lib/gridDiagnostics.ts',import.meta.url),'utf8'); for(const mark of ['navigation-click','route-state-updated','view-mounted','model-start','index-complete','dom-start','dom-complete','interaction-ready']) assert.match(app+'\n'+grid+'\n'+diagnostics,new RegExp(`grid-${mark}`)); assert.match(diagnostics,/import\.meta\.env\.DEV/); assert.doesNotMatch(diagnostics,/Event titles|calendar names/);});
