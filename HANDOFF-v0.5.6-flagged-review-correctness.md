# v0.5.6 Handoff
Repository `lifegrid-ai-planner-web` started at merge `83179f7`, which contains preceding PR #21 head `0e21fdb234b804c7241eb98652b3361eecc8d57e`. APP_VERSION/package are v0.5.6/0.5.6; AI interchange remains 4 and backup schema remains 6.

The over-flagging root cause was a broad `issue.blocking` eligibility gate; source and severity were then hard-coded as Legacy migration/Blocking in the legacy branch. The stale-row root cause was `setTimeout(refresh, 0)` reading closures before committed context collections were observable. `deriveFlaggedReviewState` now indexes records, rule codes, issue keys, record findings and groups in one pass. Unknown codes are diagnostic-only (development warning), never visible. Legacy clarification requires explicit `temporalReview: 'legacy-ambiguous'`; missing times/status alone is not ambiguity. Collection changes after the review has opened rederive current state; quick confirmations and deletes use the same path. Browser timing was not performed. Branch/commit/PR URL are filled after release finalization.
