# LifeGrid v0.5.17 release qualification
v0.5.17 freezes feature work while automated lifecycle, startup, browser, and AI-import qualification is added. It preserves interchange v4 and backup schema 7. The release blocks exact duplicate AI additions and warns on bounded title/name similarity.
