# LifeGrid v0.5.18 AI import reliability

This patch keeps filtered AI-import proposal selection canonical through final atomic validation and preserves v4 Event temporal fields during normalization. Interchange remains v4 and backups remain schema 7.
