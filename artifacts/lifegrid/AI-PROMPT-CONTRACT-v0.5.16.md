# AI prompt contract
All prompt entry points compose `aiPromptContract()` in `src/lib/aiPrompt.ts`. It specifies the exact v4 add/update envelope, entity classification, meaningful identities, sparse-update semantics, same-patch references, local floating time fields, uncertainty-to-warning policy, examples, and an internal JSON-only self-check.
